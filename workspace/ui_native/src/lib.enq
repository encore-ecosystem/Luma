extern fn luma_window_create(title: str, width: u32, height: u32) -> usize
extern fn luma_window_create_with_renderer(title: str, width: u32, height: u32,
    renderer_kind: u32,) -> usize
extern fn luma_window_destroy(handle: usize) -> bool
extern fn luma_window_reveal(handle: usize) -> bool
extern fn luma_window_open(handle: usize) -> bool
extern fn luma_window_width(handle: usize) -> u32
extern fn luma_window_height(handle: usize) -> u32
extern fn luma_window_pixel_width(handle: usize) -> u32
extern fn luma_window_pixel_height(handle: usize) -> u32
extern fn luma_window_pixel_density(handle: usize) -> f32
extern fn luma_window_present_count(handle: usize) -> u64
extern fn luma_window_read_pixel(handle: usize, x: u32, y: u32) -> u32
extern fn luma_window_platform_handle(handle: usize) -> usize
extern fn luma_direct_compositor_bind(handle: usize, compositor: usize) -> bool
extern fn luma_direct_resources_release(handle: usize) -> bool
extern fn luma_window_poll(handle: usize) -> u32
extern fn luma_window_wait(handle: usize, timeout_ms: u32) -> bool
extern fn luma_event_x(handle: usize) -> f32
extern fn luma_event_y(handle: usize) -> f32
extern fn luma_event_key(handle: usize) -> u32
extern fn luma_event_modifiers(handle: usize) -> u32
extern fn luma_event_text(handle: usize) -> str
extern fn luma_event_wheel_x(handle: usize) -> f32
extern fn luma_event_wheel_y(handle: usize) -> f32
extern fn luma_clipboard_set(value: str) -> bool
extern fn luma_clipboard_text() -> str
extern fn luma_frame_begin(handle: usize, color: u32) -> bool
extern fn luma_clip_rect(handle: usize, enabled: bool, x: f32, y: f32, width: f32, height: f32) -> bool
extern fn luma_fill_rect(handle: usize, x: f32, y: f32, width: f32, height: f32, color: u32) -> bool
extern fn luma_stroke_rect(handle: usize, x: f32, y: f32, width: f32, height: f32, color: u32) -> bool
extern fn luma_line(handle: usize, x0: f32, y0: f32, x1: f32, y1: f32, color: u32) -> bool
extern fn luma_vector_line(handle: usize, x0: f32, y0: f32, x1: f32, y1: f32, width: f32, color: u32)
-> bool
extern fn luma_vector_path_begin(handle: usize, x: f32, y: f32) -> bool
extern fn luma_vector_path_point(handle: usize, x: f32, y: f32) -> bool
extern fn luma_vector_path_stroke(handle: usize, width: f32, color: u32, closed: bool) -> bool
extern fn luma_circle(handle: usize, cx: f32, cy: f32, radius: f32, color: u32, filled: bool) -> bool
extern fn luma_round_rect(handle: usize, x: f32, y: f32, width: f32, height: f32, radius: f32, color:
    u32, filled: bool,) -> bool
extern fn luma_text(handle: usize, x: f32, y: f32, value: str, color: u32) -> bool
extern fn luma_text_wrapped(handle: usize, x: f32, y: f32, value: str, color: u32, wrap_width: f32) ->
bool
extern fn luma_font_load(handle: usize, path: str, size: f32, weight: u32) -> bool
extern fn luma_font_register(handle: usize, family: str, path: str) -> bool
extern fn luma_font_select(handle: usize, family: str, size: f32, weight: u32) -> bool
extern fn luma_font_clear(handle: usize) -> bool
extern fn luma_font_style(handle: usize, size: f32, weight: u32) -> bool
extern fn luma_text_width(handle: usize, value: str) -> f32
extern fn luma_text_height(handle: usize, value: str) -> f32
extern fn luma_text_index(handle: usize, value: str, max_width: f32) -> usize
extern fn luma_frame_end(handle: usize) -> bool
extern fn luma_direct_frame_acquire(handle: usize) -> usize
extern fn luma_direct_frame_finish(handle: usize, frame: usize) -> bool
extern fn luma_direct_composition_split(handle: usize) -> bool
extern fn luma_layout_cache_begin(handle: usize, color: u32) -> bool
extern fn luma_layout_cache_patch_begin(handle: usize, color: u32, x: f32, y: f32,
    width: f32, height: f32,) -> bool
extern fn luma_layout_cache_end(handle: usize) -> bool
extern fn luma_overlay_cache_begin(handle: usize) -> bool
extern fn luma_overlay_cache_end(handle: usize) -> bool
extern fn luma_overlay_cache_draw(handle: usize) -> bool
extern fn luma_cached_frame_begin(handle: usize, color: u32) -> bool
extern fn luma_layout_cache_ready(handle: usize) -> bool
extern fn luma_vulkan_instance(handle: usize) -> usize
extern fn luma_vulkan_physical_device(handle: usize) -> usize
extern fn luma_vulkan_device(handle: usize) -> usize
extern fn luma_vulkan_graphics_queue_family(handle: usize) -> u32
extern fn luma_external_vulkan_image_bind(handle: usize, image: u64,
    width: u32, height: u32, layout: u32,) -> bool
extern fn luma_external_vulkan_images_clear(handle: usize) -> bool
extern fn luma_external_image_draw(handle: usize, x: f32, y: f32,
    width: f32, height: f32,) -> bool
extern fn luma_backend_name() -> str
extern fn luma_renderer_name(handle: usize) -> str
extern fn luma_renderer_device_name(handle: usize) -> str
extern fn luma_backend_error() -> str
extern fn luma_backend_set_error(message: str)
extern fn luma_window_set_present_mode(handle: usize, mode: u32) -> bool
extern fn luma_path_is_directory(path: str) -> bool

pub fn native_create(title: str, width: u32, height: u32) -> usize {
    unsafe {
        ret luma_window_create(title, width, height)
    }
}
pub fn native_create_with_renderer(title: str, width: u32, height: u32,
    renderer_kind: u32,) -> usize {
    unsafe {
        ret luma_window_create_with_renderer(title, width, height, renderer_kind)
    }
}
pub fn native_destroy(handle: usize) -> bool {
    unsafe {
        ret luma_window_destroy(handle)
    }
}
pub fn native_reveal(handle: usize) -> bool {
    unsafe {
        ret luma_window_reveal(handle)
    }
}
pub fn native_open(handle: usize) -> bool {
    unsafe {
        ret luma_window_open(handle)
    }
}
pub fn native_width(handle: usize) -> u32 {
    unsafe {
        ret luma_window_width(handle)
    }
}
pub fn native_height(handle: usize) -> u32 {
    unsafe {
        ret luma_window_height(handle)
    }
}
pub fn native_pixel_width(handle: usize) -> u32 {
    unsafe {
        ret luma_window_pixel_width(handle)
    }
}
pub fn native_pixel_height(handle: usize) -> u32 {
    unsafe {
        ret luma_window_pixel_height(handle)
    }
}
pub fn native_pixel_density(handle: usize) -> f32 {
    unsafe {
        ret luma_window_pixel_density(handle)
    }
}
pub fn native_present_count(handle: usize) -> u64 {
    unsafe {
        ret luma_window_present_count(handle)
    }
}
pub fn native_read_pixel(handle: usize, x: u32, y: u32) -> u32 {
    unsafe {
        ret luma_window_read_pixel(handle, x, y)
    }
}
pub fn native_platform_handle(handle: usize) -> usize {
    unsafe {
        ret luma_window_platform_handle(handle)
    }
}
pub fn native_direct_compositor_bind(handle: usize, compositor: usize) -> bool {
    unsafe {
        ret luma_direct_compositor_bind(handle, compositor)
    }
}
pub fn native_direct_resources_release(handle: usize) -> bool {
    unsafe {
        ret luma_direct_resources_release(handle)
    }
}

pub fn native_vulkan_instance(handle: usize) -> usize {
    unsafe {
        ret luma_vulkan_instance(handle)
    }
}
pub fn native_vulkan_physical_device(handle: usize) -> usize {
    unsafe {
        ret luma_vulkan_physical_device(handle)
    }
}
pub fn native_vulkan_device(handle: usize) -> usize {
    unsafe {
        ret luma_vulkan_device(handle)
    }
}
pub fn native_vulkan_graphics_queue_family(handle: usize) -> u32 {
    unsafe {
        ret luma_vulkan_graphics_queue_family(handle)
    }
}
pub fn native_external_vulkan_image_bind(handle: usize, image: u64,
    width: u32, height: u32, layout: u32,) -> bool {
    unsafe {
        ret luma_external_vulkan_image_bind(handle, image, width, height, layout)
    }
}
pub fn native_external_vulkan_images_clear(handle: usize) -> bool {
    unsafe {
        ret luma_external_vulkan_images_clear(handle)
    }
}
pub fn native_external_image_draw(handle: usize, x: f32, y: f32,
    width: f32, height: f32,) -> bool {
    unsafe {
        ret luma_external_image_draw(handle, x, y, width, height)
    }
}
pub fn native_set_present_mode(handle: usize, mode: u32) -> bool {
    unsafe {
        ret luma_window_set_present_mode(handle, mode)
    }
}
pub fn native_poll(handle: usize) -> u32 {
    unsafe {
        ret luma_window_poll(handle)
    }
}
pub fn native_wait(handle: usize, timeout_ms: u32) -> bool {
    unsafe {
        ret luma_window_wait(handle, timeout_ms)
    }
}
pub fn native_event_x(handle: usize) -> f32 {
    unsafe {
        ret luma_event_x(handle)
    }
}
pub fn native_event_y(handle: usize) -> f32 {
    unsafe {
        ret luma_event_y(handle)
    }
}
pub fn native_event_key(handle: usize) -> u32 {
    unsafe {
        ret luma_event_key(handle)
    }
}
pub fn native_event_modifiers(handle: usize) -> u32 {
    unsafe {
        ret luma_event_modifiers(handle)
    }
}
pub fn native_event_text(handle: usize) -> str {
    unsafe {
        ret luma_event_text(handle)
    }
}
pub fn native_event_wheel_x(handle: usize) -> f32 {
    unsafe {
        ret luma_event_wheel_x(handle)
    }
}
pub fn native_event_wheel_y(handle: usize) -> f32 {
    unsafe {
        ret luma_event_wheel_y(handle)
    }
}
pub fn native_clipboard_set(value: str) -> bool {
    unsafe {
        ret luma_clipboard_set(value)
    }
}
pub fn native_clipboard_text() -> str {
    unsafe {
        ret luma_clipboard_text()
    }
}
pub fn native_begin(handle: usize, color: u32) -> bool {
    unsafe {
        ret luma_frame_begin(handle, color)
    }
}
pub fn native_clip(handle: usize, enabled: bool, x: f32, y: f32, width: f32, height: f32) -> bool {
    unsafe {
        ret luma_clip_rect(handle, enabled, x, y, width, height)
    }
}
pub fn native_fill_rect(handle: usize, x: f32, y: f32, width: f32, height: f32, color: u32) -> bool {
    unsafe {
        ret luma_fill_rect(handle, x, y, width, height, color)
    }
}
pub fn native_stroke_rect(handle: usize, x: f32, y: f32, width: f32, height: f32, color: u32) -> bool
{
    unsafe {
        ret luma_stroke_rect(handle, x, y, width, height, color)
    }
}
pub fn native_line(handle: usize, x0: f32, y0: f32, x1: f32, y1: f32, color: u32) -> bool {
    unsafe {
        ret luma_line(handle, x0, y0, x1, y1, color)
    }
}
pub fn native_vector_line(handle: usize, x0: f32, y0: f32, x1: f32, y1: f32, width: f32, color: u32)
-> bool {
    unsafe {
        ret luma_vector_line(handle, x0, y0, x1, y1, width, color)
    }
}
pub fn native_vector_path_begin(handle: usize, x: f32, y: f32) -> bool {
    unsafe {
        ret luma_vector_path_begin(handle, x, y)
    }
}
pub fn native_vector_path_point(handle: usize, x: f32, y: f32) -> bool {
    unsafe {
        ret luma_vector_path_point(handle, x, y)
    }
}
pub fn native_vector_path_stroke(handle: usize, width: f32, color: u32, closed: bool) -> bool {
    unsafe {
        ret luma_vector_path_stroke(handle, width, color, closed)
    }
}
pub fn native_circle(handle: usize, cx: f32, cy: f32, radius: f32, color: u32, filled: bool) -> bool
{
    unsafe {
        ret luma_circle(handle, cx, cy, radius, color, filled)
    }
}
pub fn native_round_rect(handle: usize, x: f32, y: f32, width: f32, height: f32, radius: f32, color:
    u32, filled: bool,) -> bool {
    unsafe {
        ret luma_round_rect(handle, x, y, width, height, radius, color, filled)
    }
}
pub fn native_text(handle: usize, x: f32, y: f32, value: str, color: u32) -> bool {
    unsafe {
        ret luma_text(handle, x, y, value, color)
    }
}
pub fn native_text_wrapped(handle: usize, x: f32, y: f32, value: str, color: u32, wrap_width: f32) ->
bool {
    unsafe {
        ret luma_text_wrapped(handle, x, y, value, color, wrap_width)
    }
}
pub fn native_font_load(handle: usize, path: str, size: f32, weight: u32) -> bool {
    unsafe {
        ret luma_font_load(handle, path, size, weight)
    }
}
pub fn native_font_register(handle: usize, family: str, path: str) -> bool {
    unsafe {
        ret luma_font_register(handle, family, path)
    }
}
pub fn native_font_select(handle: usize, family: str, size: f32, weight: u32) -> bool {
    unsafe {
        ret luma_font_select(handle, family, size, weight)
    }
}
pub fn native_font_clear(handle: usize) -> bool {
    unsafe {
        ret luma_font_clear(handle)
    }
}
pub fn native_font_style(handle: usize, size: f32, weight: u32) -> bool {
    unsafe {
        ret luma_font_style(handle, size, weight)
    }
}
pub fn native_text_width(handle: usize, value: str) -> f32 {
    unsafe {
        ret luma_text_width(handle, value)
    }
}
pub fn native_text_height(handle: usize, value: str) -> f32 {
    unsafe {
        ret luma_text_height(handle, value)
    }
}
pub fn native_text_index(handle: usize, value: str, max_width: f32) -> usize {
    unsafe {
        ret luma_text_index(handle, value, max_width)
    }
}
pub fn native_end(handle: usize) -> bool {
    unsafe {
        ret luma_frame_end(handle)
    }
}
pub fn native_direct_frame_acquire(handle: usize) -> usize {
    unsafe {
        ret luma_direct_frame_acquire(handle)
    }
}
pub fn native_direct_composition_split(handle: usize) -> bool {
    unsafe {
        ret luma_direct_composition_split(handle)
    }
}
pub fn native_direct_frame_finish(handle: usize, frame: usize) -> bool {
    unsafe {
        ret luma_direct_frame_finish(handle, frame)
    }
}
pub fn native_layout_cache_begin(handle: usize, color: u32) -> bool {
    unsafe {
        ret luma_layout_cache_begin(handle, color)
    }
}
pub fn native_layout_cache_patch_begin(handle: usize, color: u32, x: f32, y: f32,
    width: f32, height: f32,) -> bool {
    unsafe {
        ret luma_layout_cache_patch_begin(handle, color, x, y, width, height)
    }
}
pub fn native_layout_cache_end(handle: usize) -> bool {
    unsafe {
        ret luma_layout_cache_end(handle)
    }
}
pub fn native_overlay_cache_begin(handle: usize) -> bool {
    unsafe {
        ret luma_overlay_cache_begin(handle)
    }
}
pub fn native_overlay_cache_end(handle: usize) -> bool {
    unsafe {
        ret luma_overlay_cache_end(handle)
    }
}
pub fn native_overlay_cache_draw(handle: usize) -> bool {
    unsafe {
        ret luma_overlay_cache_draw(handle)
    }
}
pub fn native_cached_frame_begin(handle: usize, color: u32) -> bool {
    unsafe {
        ret luma_cached_frame_begin(handle, color)
    }
}
pub fn native_layout_cache_ready(handle: usize) -> bool {
    unsafe {
        ret luma_layout_cache_ready(handle)
    }
}
pub fn native_backend_name() -> str {
    unsafe {
        ret luma_backend_name()
    }
}
pub fn native_renderer_name(handle: usize) -> str {
    unsafe {
        ret luma_renderer_name(handle)
    }
}
pub fn native_renderer_device_name(handle: usize) -> str {
    unsafe {
        ret luma_renderer_device_name(handle)
    }
}
pub fn native_backend_error() -> str {
    unsafe {
        ret luma_backend_error()
    }
}
pub fn native_backend_set_error(message: str) {
    unsafe {
        luma_backend_set_error(message)
    }
}
pub fn native_path_is_directory(path: str) -> bool {
    unsafe {
        ret luma_path_is_directory(path)
    }
}
